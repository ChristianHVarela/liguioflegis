import { Champion } from "../types/champion";

async function getChampions(name: string, localization: string, className: string, gender: string, offset: string, limit: string): Promise<void> {
  // implementação da função

}

export default { getChampions }